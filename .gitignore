# macOS
.DS_Store

# Claude Code workspace
.claude/

# Large pitch PDF (exceeds GitHub's 100MB limit; kept locally only)
Subtext Pitch.pdf

# Root-level font duplicates — the live copies live in notably-site/assets/fonts/
/*.otf
.vercel
